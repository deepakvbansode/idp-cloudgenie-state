apiVersion: apps/v1
kind: Deployment
metadata:
  name: repo-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: repo
  template:
    metadata:
      labels:
        app: repo
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: repo-service
spec:
  selector:
    app: repo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
